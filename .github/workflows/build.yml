name: ci
on:
  push:
    branches:
      - 'main'

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - image: 'bsmeding/nautobot'
            dockerfile: v1/Dockerfile
            nb_version: 1.6.20
            py_version: 3.9
          # - image: 'bsmeding/nautobot'
          #   dockerfile: v2/Dockerfile
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      # - name: Extract metadata (tags, labels) for Docker
      #   id: meta
      #   uses: docker/metadata-action@v4
      #   with:
      #     images: ${{ matrix.image }}
      # - name: Set variables
      #   # run: |
      #   #   NB_V1_VER=$(cat ${{ github.workspace }}/v1/VERSION)
      #   #   echo "NB_V1_VER=$NB_V1_VER" >> $GITHUB_ENV
      #   #   PYTHON_V1_VER=$(cat ${{ github.workspace }}/v1/PYTHON_VERSION)
      #   #   echo "PYTHON_V1_VER=$PYTHON_V1_VER" >> $GITHUB_ENV
      #   #   NB_V2_VER=$(cat ${{ github.workspace }}/v2/VERSION)
      #   #   echo "NB_V2_VER=$NB_V2_VER" >> $GITHUB_ENV
      #   #   PYTHON_V2_VER=$(cat ${{ github.workspace }}/v2/PYTHON_VERSION)
      #   #   echo "PYTHON_V2_VER=$PYTHON_V2_VER" >> $GITHUB_ENV
      #   run: |
      #     echo "VERSION=${{ github.run_number }}" >> $GITHUB_ENV
      #     echo "NB_VERSION=${{ matrix.nb_version }}" >> $GITHUB_ENV
      #     echo "PY_VERSION=${{ matrix.py_version }}" >> $GITHUB_ENV  
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ${{ github.workspace }}
          file: ${{ matrix.dockerfile }} --build-arg PYTHON_VER=${{ matrix.py_version }} --build-arg NAUTOBOT_VER=${{ matrix.nb_version }}
          push: true
          tags: |
            ${{ matrix.image }}:${{ matrix.nb_version }}
